import { makeStyles, TableRow, withStyles } from "@material-ui/core";

const MyStageGamesStyle = makeStyles((theme) => ({
    table: {
     
    },
    tableContainer:{

      paddingTop:30,
      // paddingLeft:150,
      // paddingRight:150
    }    
  }));
